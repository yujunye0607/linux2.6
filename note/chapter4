关于中断
软中断：指发生在中断下半部 不紧急的中断，表现为可延时函数
硬中断：指在中断上半部 硬件触发的 必须马上执行的中断 如检测到网卡数据包

内部中断：CPU内部执行指令发出的中断，“同步中断” ≈ “内部中断”
外部中断：CPU外部的中断，如IO设备发出的中断，注意：尽管有些定时器在CPU内部 但逻辑上仍是外部中断

同步中断：CPU自己发出的中断，包括故障（PC寄存器存放当前指令，下一条指令会重复执行当前指令），陷阱（PC存放下一条指令），异常（异常中止，编程异常），
         一些常用的如缺页异常，系统调用等属于同步中断
异步中断：IO设备，定时器等触发的中断

软中断，工作队列，tasklet：
         tasklet基于软中断，但是是串行执行，对于一些外设驱动的下半部可以使用tasklet，如SD卡，触摸屏
         软中断是并行的，所以有快速响应需求应使用软中断，如网卡收发数据包，高精度定时器等
         工作队列不同于中断，类似于中断，是运行在内核态的内核进程维护，持有的是进程上下文，可以谨慎的休眠，处理一些可以延时处理的请求，如磁盘IO，文件系统